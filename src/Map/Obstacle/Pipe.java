package Map.Obstacle;

import javax.swing.ImageIcon;

public class Pipe extends Obstacle {
    public Pipe(int x, int y, int width, int height, ImageIcon icon) {
        super(x, y, width, height, icon);
    }
}
